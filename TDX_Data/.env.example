# Copy this file to `.env` and fill in your real TDX credentials.
# `.env` is git-ignored; this `.env.example` is the safe-to-commit template.
TDX_CLIENT_ID=your-tdx-client-id
TDX_CLIENT_SECRET=your-tdx-client-secret
