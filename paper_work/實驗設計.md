# 實驗設計 — 雙軌驗證架構

> **目的**：定義本專題的訓練／驗證方法、資料切分、基準對照與評估協定，作為期末報告的「研究方法」章節與實作依據
> **對照文件**：`115_期末專題計劃書.pdf`（§4.2 資料、§4.3 預測、§4.4 決策、§4.6 評估指標、§5 預期結果）
> **相關文件**：`實驗記錄_DRL決策模組.md`（已完成的實驗歷程與所有實測數字）、`整合交接文件_DRL決策模組.md`
> **版本**：2026-08-27（**v5**：Gated Fusion（計畫書 §4.3）完成並接進決策層、
> HA 基準補齊、S3 主幹道封閉情境完成、beam search 解碼使 ⑦ 在兩個情境下都完全可比。
> 本版是**第一次所有主線實驗都有 10 seeds 的正式數字**）

> **v4 → v5 的主要更新**（v4 為 2026-08-21）：
>
> | 項目 | v4 | **v5** |
> |---|---|---|
> | HA 基準（計畫書 §4.6） | ❌ 未實作 | ✅ **完成**（§3.6），並帶出三個關於資料性質的發現 |
> | Gated Fusion（計畫書 §4.3） | ❌ 未實作，僅有動機 | ✅ **完成**（§3.7）：聯合訓練雙路，一個模型輸出 15/30/60 |
> | 集成權重 | 僅在驗證集挑選 | ✅ **測試集確認**（0.20 vs 0.15，差 0.07%） |
> | S3 主幹道封閉 | ❌ 三個選項未定案 | ✅ **完成並跑完 10 seeds**（§4.3、§4.7） |
> | ⑦ 的可比性 | S2 served 97.5%；S3 未做 | ✅ **beam-8 解碼：S2 99.9%、S3 96.0%**（§4.5、§4.6） |
> | 決策層吃的預測 | `0.2/0.8` 固定權重 | ✅ **gated fusion**，且 agent 以新觀測重訓（§5） |
> | worst-ρ 是否達標 | ⚠️ 恰在門檻（−19.8±2.6%） | ✅ **達標**（S2 −24.6%、S3 −27.5%） |
>
> **v4 中兩處需要更正的讀法**（不是筆誤，是當時的推論被後續資料推翻）：
>
> 1. §4.7 曾把 ①②③ 相對 ④ 的 `±0.2%` 讀成「塌陷極度穩定」。**那是簡併的假象**——
>    `0.2/0.8` 讓 ④ 的路徑逐條等於 ③。fusion 打破後真實變異是 **±2.1%**（§4.7 ⑤）。
> 2. §4.8 曾推測「to-go 修好後剩餘差距可能是 search error」。**beam search 否證了它**
>    ——加寬 4 倍只讓 ATT 動 0.8%（§4.8 ⑥）。

---

## 1. 設計原則

### 1.1 核心認知：本專題有**兩個獨立的驗證**，不是一個

| | 驗證什麼 | 需要什麼 | 指標 | 可用真實資料驗證？ |
|---|---|---|---|---|
| **軌道 A：預測** | 模型預測車速準不準 | 真實時序資料 | MAE / RMSE / MAPE | ✅ 可以 |
| **軌道 B：決策** | 全域懲罰能否抑制羊群 | 路網 + OD 需求 + 壅塞模型 | ATT / Gini / worst-ρ | ❌ **本質上不行** |

**軌道 B 為何無法用真實資料驗證**（此為方法論的必然，非本專題的妥協）：

1. **沒有真實 OD 資料**——TDX 提供的是「路段速度」，不是「誰從哪裡到哪裡」。
2. **需要反事實（counterfactual）**——要證明「抑制了羊群」，必須比較「若這 800 台車改走別條路會如何」。真實世界不提供平行時空。

因此軌道 B 的正確做法是：**環境用真實的（路網拓樸、速限、TDX 車速、車道數→容量），需求用合成的，並在完全相同的環境與需求下比較各策略**。這是交通模擬研究的標準做法（SUMO 類研究皆然），在報告中應主動說明，而非迴避。

### 1.2 被排除的方案與理由

曾考慮但排除的兩個方案，皆因**預測模型無法跨路網遷移**：

| 模型 | 節點數綁定處 | 後果 |
|---|---|---|
| STGCN | `nn.LayerNorm([n_vertex, channels])`（`layers.py:246`）＋ spectral GCN 綁定固定 Laplacian | 設計上即為 transductive |
| STGAT | 逐節點 `nn.Parameter(torch.zeros(num_nodes, out_features))`（`stgat.py:37`）＋ `BatchNorm2d(num_nodes)`（`stgat.py:80`） | GAT 理論上可 inductive，但此實作被逐節點參數破壞 |

→ **以 207 節點訓練的權重無法載入不同節點數的模型（shape mismatch）**，故「METR-LA 訓練預測 → 直接套用台中路網」不可行。計畫書 §5 的 zero-shot 遷移主張在現有實作下不成立；改以 §4.3 的 **fine-tune 對照實驗**（A3）取代並驗證。

### 1.3 ★ 三層路網架構（v4 新增）

v3 假設「一張裁切後的路網同時服務預測與決策」。實作後發現**兩者對地圖大小的需求相反**：

| | 需要什麼 | 為什麼 |
|---|---|---|
| **軌道 A（預測）** | 地圖**越大越好** | 預測的圖建在 **TDX 路段**上，地圖只決定「哪些路段通過 map-matching」與高斯核的路網距離。地圖小 → 通過匹配的路段少 → **`n_vertex` 永久受限，事後換大地圖也補不回來** |
| **軌道 B（決策）** | 場域**要小且集中於 TDX 走廊** | RL 的 credit assignment 隨路徑長度惡化；且覆蓋率的分母是場域邊數 |

因此改為三層，各司其職：

```
① 完整圖   Map_fined  39,920 節點 / 43,711 段（1,748.8 km）   ← 原始交付，Demo 繪圖
      │ build_simplified_network.py：依 oneway 展開 → 取最大 SCC → 合併中途節點
      ▼
② 簡化圖   9,904 節點 / 27,022 有向邊                          ← 軌道 A 的 map-matching 與路網距離
      │ build_arena.py：主幹道種子 → 走廊 → 連通補齊 → 再簡化
      ▼
③ 實驗場域 840 節點 / 1,690 有向邊                             ← 軌道 B 的訓練與評估
```

> **節點 id 全程沿用原始 OSM id**，故場域上算出的路徑可直接畫在①的完整地圖上，Demo 不需另做對應。

---

## 2. 資料集

### 2.1 三份資料

| 資料 | 內容 | 用途 | 狀態 |
|---|---|---|---|
| **METR-LA** | 207 sensors，5 分鐘聚合，2012/3–2012/6，34,272 步 | 軌道 A 的**文獻可比 benchmark** | ✅ 已完成 |
| **台中 TDX** | **202 個 section**（244 → 202，見下），**50,283 步**（2026-01-27 ~ 07-21，**5 分鐘間隔**） | 軌道 A 的**本地資料** + 軌道 B 的邊速度來源 | ✅ 已完成前處理 |
| **台中 OSM 路網** | `Map_fined` **39,920 節點 / 43,711 段**（v3 交付，2026-08-13） → 簡化 **9,904 / 27,022** → 場域 **840 / 1,690** | 軌道 B 的路由環境；軌道 A 的路網距離 | ✅ 已完成（§1.3） |

**已驗證的資料屬性**（2026-08-13 重建後實測，來源 `Map/speed_meta.json`）：

| 項目 | 實測值 | 判讀 |
|---|---|---|
| 原始規模 | **28,023,445 行 / 176 天 / 11.9 GB** | — |
| 取樣間隔 | **5 分鐘** | ✅ 與 METR-LA 一致；12 步 = 1 小時慣例可直接沿用 |
| 速度單位 | **km/h**（mean 24.2、median 21） | 與 METR-LA 的 mph 不同，**下游一律以 km/h 計算** |
| **原始無效值** | **67.94%** 的 `TravelSpeed=0` 且 `TravelTime=0`（ETag 未配對到車輛的佔位值，非真實壅塞） | 🔴 已轉為缺值 |
| 異常值 | max **192 km/h**，**0.062%** 超過 120 | ✅ 已處理：>120 km/h **轉為缺值**（非 clip，理由見 §2.3） |
| **降採樣後仍缺** | **31.2%** | 🔴 為 METR-LA（7.13%）的 4.4 倍 |
| **剔除高缺值路段後** | **補值率 24.6%**（244 → 202 個路段） | 🔴 仍為 METR-LA 的 3.5 倍，**評估必須用 mask** |
| 時區 | **UTC (+00:00)** | ⚠️ 台灣為 UTC+8，尖峰時段分析前必須轉換 |
| 資料期間 | 2026-01-27 15:55 ~ 2026-07-21 06:05（UTC） | 176 天 |
| 路網連通性 | 簡化後最大 SCC = 全部 9,904 節點（僅取 SCC） | ✅ 已保證強連通 |

**節點數的兩段篩選**（v3 為三段，因舊地圖框選不足而多了一段）：

```
244 個 TDX 路段
  │ −0   全數通過 300 m map-matching 門檻 ★ v3 為 212（舊地圖東界短 3.1 km、缺北屯區）
  ▼
244 個
  │ −42  缺值率 > 50%（實際落在 51.1%–89.4%，無完全無資料者）
  ▼
202 個 ← 最終進入訓練的節點數
```

> **v3 → v4 為何多了 27 個路段**：舊 `Map/` 的框選範圍東界比 TDX 路段的 bbox 短 3.1 km，
> 且 `Map_fined` v1/v2 完全缺北屯區（崇德路 53、松竹路 31、中清路 30 共 114 個路段落空）。
> v3 版地圖擴大框選後 **244 / 244 全數匹配（100%）**，存活路段由 175 升至 **202（+15.4%）**。
> 🔴 **教訓：地圖框選範圍會永久決定 `n_vertex` 的上限**——事後換大地圖也救不回已經沒被匹配的路段。

### 2.2 台中資料集與 METR-LA 的規模對照

| | METR-LA | 台中 TDX |
|---|---|---|
| 節點數 | 207 | **202** |
| 時間步數 | 34,272 | **50,283** |
| 缺失／補值率 | 7.13% | **24.6%** |

1. **架構幾乎不用改**：僅需將 `n_vertex` 由 207 改為 **202**（`STGCN/script/dataloader.py`），其餘超參數（`nheads=4`、`nhid=64`、4 層 ST-Block）皆在已驗證可行的規模內。
   ⚠️ **該值寫死、鄰接矩陣從磁碟載入，兩者不一致會 shape mismatch**——每次重建資料鏈後務必複查。
2. **資料量優於 METR-LA**：50,283 > 34,272 步，訓練資料充足。
3. **使 A3（遷移實驗）可行**：202 vs 207 幾乎同規模；且 A3 本來就要**重新初始化**節點綁定參數。
4. ⚠️ **但資料品質明顯較差**：補值率是 METR-LA 的 3.5 倍，這是台中版 MAE 不能與 METR-LA／論文直接並列的原因——必須以 mask 計分並揭露補值比例（§2.3、§3.3）。

### 2.3 資料切分與缺失值處理

| 資料 | 切分 | 標準化 |
|---|---|---|
| METR-LA | 70% train / 10% val / 20% test（**依時間先後，不打散**） | Z-score（以 train 統計量） |
| 台中 TDX | 同上（50,283 步 → 約 35,198 / 5,028 / 10,057） | Z-score（以 train 統計量） |

#### 🔴 資料品質鏈與 mask 評估協定（本設計最重要的方法論註記）

```
原始 TravelSpeed（28,023,445 行）
  │  67.94% 無效佔位值（TravelSpeed=0 且 TravelTime=0，ETag 未配對到車輛）→ NaN
  │   0.06% 超過 120 km/h（感測器異常，實測 max 192）                     → NaN
  ▼  5 分鐘降採樣取平均（窗口內若仍有真實值即可救回一部分）
  │  仍有 31.2% 缺值
  ▼  剔除缺值 > 50% 的路段（244 → 202 個）
  │  補值率降至 24.6%
  ▼  三層補值：ffill → bfill → 該路段全域平均
taichung_vel.csv（50,283 × 202，表面上 0% 缺失）
```

> **異常值為何轉缺值而非 clip**：clip 會留下一個「捏造的觀測」並被 mask 標成 `True`，
> 等於拿假資料去評分；轉缺值則交由補值機制處理，且 mask 正確標成 `False`。

**對照 METR-LA 只有 7.13% 缺失，台中是其 3.5 倍。** 三個後果與處置：

| 後果 | 處置 |
|---|---|
| **24.6% 的儲存格是填補值**，計入 MAE 會使成績虛低，且無法與 METR-LA／論文並列比較 | ✅ **`build_speed.py` 輸出 `taichung_mask.npy`**（補值前取得，`True` = 真實觀測）。**軌道 A 一律只在 `mask=True` 的位置計分**（實際計分比例 **73.8%**） |
| **ffill 造成大量「與前一步相同」的樣本** → 獎勵 persistence-like 的偷懶行為 | ✅ **軌道 A 必須保留 persistence 對照**（§3.2）——`實驗記錄` §5 正是靠它揭穿模型沒在學習 |
| **部分路段幾乎全是補值**，其「預測」沒有意義 | ✅ **已剔除缺值 > 50% 的 42 個路段**（244 → **202**） |

> ⚠️ **保留下來的 202 個路段品質仍不均**，最低者接近 50% 門檻。若後續 MAE 表現異常，這是第一個該查的地方。

> **與 METR-LA 的機制差異（不可混用）**：METR-LA 以 `0` 表示缺失且**保留原值**，故遮罩靠 loss 的 `null_val=0.0`（見 `實驗記錄` §5.3 Bug 1，**呼叫端務必明確傳入**）。台中資料已預先填補、**沒有 0 可遮罩**，必須改用外部 `mask.npy`。

> ✅ **已實作**：`STGCN/evaluate_masked.py` 與 `STGAT/evaluate_masked_taichung.py`。
> 兩支工具由**完全獨立**的資料載入／切分／遮罩邏輯寫成，算出的 persistence 基準
> **吻合到 0.1% 以內**（4.2872/4.6744/5.1281 vs 4.2852/4.6712/5.1233，差異僅來自視窗數 7,527 vs 7,539）——
> 這是目前最強的評估流程交叉驗證。

**報告揭露要求**：需明確寫出「原始無效值 67.9% → 降採樣後缺值 31.2% → 最終補值比例 24.6%」，以及「MAE/RMSE/MAPE 僅在真實觀測位置（73.8%）計算」。隱瞞此點會使軌道 A 的數字失去可信度。

### 2.4 鄰接矩陣建構

沿用計畫書 §4.2：Gaussian kernel 加權
$$W_{ij} = \exp\left(-\frac{d_{ij}^2}{\sigma^2}\right) \text{ if } d_{ij} \le \kappa$$

其中 $d_{ij}$ 為**沿道路的最短距離**（非歐式直線距離）——以 Dijkstra 在**簡化圖（9,904 節點）**上計算，符合計畫書「以路網距離計算」的要求。

**距離門檻 κ 的決定方式**：原始實作未套用 `if d_ij ≤ κ`，導致鄰接矩陣**全部非零（100% 稠密）**；全連通會讓 graph convolution 在所有節點上平均，**稀釋空間結構**。現改為**自動挑選 κ 使鄰接密度對齊 METR-LA**：

| | 節點數 | σ | κ | **非零比例** |
|---|---|---|---|---|
| METR-LA（參照） | 207 | — | — | **37.3%** |
| 台中（修正前） | 212 | 自動 | 無 | ❌ 100% |
| 台中 v3（舊地圖，175 路段） | 175 | 2,038.2 m | 3,025.5 m | 40.0% |
| **台中 v4（`Map_fined`，202 路段）** | **202** | **2,283.4 m** | **3,341.8 m** | ✅ **37.56%**（目標 37.3%） |

> 對齊密度的意義：讓兩個資料集在「每個節點平均有多少鄰居」這件事上條件相當，A1／A2 的比較才不會因為圖的稠密程度不同而失真。σ 沿用原設定（可達距離的標準差），κ 由 `build_network.py --target-density 0.373` 自動求得。**可達節點對比例 100%**。
>
> **v4 為何比 v3 更貼近目標**：v3 的 κ 是在 212 路段上決定後才剔除 37 個，倖存者分佈變集中使密度回升到 40.0%；v4 因 244 路段全數匹配，κ 的求解基底與最終集合一致，落在 **37.56%**（誤差 0.26 個百分點）。

### 2.5 資料處理 pipeline（v4）

依**靜態幾何／動態時序**切分，使調整 σ／κ 不必重讀 11.9 GB 原始檔：

```
① Capture_Road_Node.py            → Map/Map_fined/*.csv                  (OSM 路網，組員 A 維護)
② fetch_tdx_section_metadata.py   → tdx_section_metadata.csv             (244 個路段座標)
③ fetch_tdx_section_live.py       → tdx_section_live_raw.jsonl           (11.9 GB，176 天)

④ build_simplified_network.py ← ① 【★ v4 新增：39,920 → 9,904 節點】
     ├─ simplified_nodes/edges_taichung.csv   一列一條有向邊
     └─ simplified_meta.json                  含旅行時間不變性證明

⑤ build_network.py  ← ②+④        【靜態幾何，只讀 CSV，數秒可重跑】
     ├─ section_to_edge_mapping.csv    一對一（沿用原欄位，向下相容）
     ├─ section_to_edges.csv           一對多（244 路段 → 715 條邊）
     ├─ taichung_dist.npy              244×244 沿道路距離（Dijkstra on 9,904 節點）
     ├─ taichung_section_index_full.csv
     └─ network_meta.json              σ／κ／覆蓋率 → 供 ⑥ 沿用

⑥ build_speed.py    ← ③+⑤        【動態時序，不再需要 OSM 檔案】
     ├─ taichung_vel.csv               (50,283 × 202) 車速矩陣
     ├─ taichung_mask.npy              True = 真實觀測、False = 補值
     ├─ taichung_adj.npy               套用 ⑤ 的 σ／κ，並對齊篩選後的路段集合
     ├─ taichung_section_index.csv / taichung_timestamps.csv
     └─ speed_meta.json                缺值統計、被剔除的 42 個路段

⑦ convert_to_stgcn_dataset.py / convert_to_stgat_dataset.py ← ⑥
     → STGCN/data/taichung/{vel.csv, adj.npz, mask.npy, ...}、STGAT 對應格式

⑧ build_arena.py    ← ④+⑤        【★ v4 新增：決策層實驗場域，9,904 → 840 節點】
     ├─ arena_nodes/edges_taichung.csv
     └─ arena_meta.json                覆蓋率（按邊／按長度）、Demo 端點檢查
```

**★ 一對多對應（`section_to_edges.csv`）**：一個 TDX 路段是「一整段路」，實際橫跨數個路口。作法為把路段起訖點各自貼到最近路口，再求路網最短路徑，展開成 edge 序列。實測 **237/244 成功還原路徑**（7 個退回單一邊）。

| | 邊數 |
|---|---:|
| 244 個路段展開後的**唯一邊** | **715** |
| 其中屬於 **202 個存活路段**者（決策層實際可用的「種子邊」） | **601** |
| 落在 840 節點場域內者 | **416** |

> 601 與 715 的差額 114 條，屬於被剔除的 42 個高缺值路段——它們有幾何對應但沒有可信的預測。

#### ★ `build_simplified_network.py` 的三個設計要點（v4 新增）

`Map_fined` 的節點粒度極細（每 ~44 m 一個節點），其中 **73.2% 是「一進一出」的中途節點**——逐節點決策的 policy 在那裡沒有選擇可做，卻要付出一次決策、一次編碼、一次 PPO transition。

1. **先依 `oneway` 展開再判斷中途節點**——節點算不算「中途」取決於方向。
   盲目雙向化會憑空生出約三分之一的逆向邊，讓分流看起來比實際容易。
2. **速限用「長度加權調和平均」** $S = \sum \ell_i / \sum (\ell_i / v_i)$——**唯一能讓 `t0` 完全不變的算法**。算術平均或取最小會讓全網每條邊的 `t0` 悄悄偏移。
3. **輸出改為一列一條有向邊**——`taichung_loader.py` 不讀 `oneway` 欄，沿用「一列一路段」會讓它把每條路都當單行道。

**內建正確性證明**：比對原圖與簡化圖之間 **300 組 OD** 的自由流最短路徑時間，實測**最大相對誤差 1.51e-15**（機器精度）、0 組不可達；超過 `1e-6` 直接 `exit 1`。另設「不可達比例 > 2% 直接失敗」的連通性硬檢查。

> **為何需要第二道檢查**：第一版的孤兒環偵測把**被正確合併掉的鏈中間節點**誤判成孤兒又加回去，
> 等於撤銷簡化（300 組 OD 有 248 組不可達），卻仍印出 `PASS`。
> **檢查只跑在沒壞的部分上，會給出假的通過訊號。**

---

## 3. 軌道 A：預測實驗

### 3.1 三組實驗

| # | 實驗 | 訓練資料 | 驗證資料 | 目的 | 狀態 |
|---|---|---|---|---|---|
| **A1** | METR-LA baseline | METR-LA train | METR-LA test | **與文獻可比**的性能基準 | ✅ 完成（§3.4） |
| **A2** | TDX from scratch | TDX train | TDX test | 本地適用性 | ✅ 完成（§3.5） |
| **A3** | METR-LA → TDX fine-tune | METR-LA 預訓練 → TDX train | TDX test | **可移轉性驗證**（計畫書 §5） | 🔴 **未做——目前唯一完全未動的計畫書要求** |

**A3 的實作方式**：載入 METR-LA checkpoint 中**非節點綁定**的權重（TimeBlock／GTCN、GAT 的 `W`／`a1`／`a2`、EndConv），**重新初始化**節點綁定參數（`bias[num_nodes, out]`、`BatchNorm2d(num_nodes)`、`LayerNorm([n_vertex, ...])`），再以 TDX 資料 fine-tune。
**判讀**：若 A3 較 A2 收斂更快或最終 MAE 更低 → 支持「時空預測知識可跨城市遷移」的主張。

### 3.2 每組實驗的模型對照

| 模型 | 說明 | 狀態 |
|---|---|---|
| HA（Historical Average） | 最簡統計基準 | ✅ **完成**（§3.6）；`ha_baseline.py`，不需 checkpoint 或 GPU |
| **Persistence** | 直接以最後一幀當預測（**強制納入**——`實驗記錄` §5 證明它是偵測「模型是否真的在學」最有效的診斷工具） | ✅ |
| STGCN | 單一模型（三時程各訓一次） | ✅ |
| STGAT | 單一模型 | ✅ |
| **Fusion（本系統）** | 計畫書 §4.3 的雙路閘控融合，**一個模型輸出 15/30/60** | ✅ **完成**（§3.7） |
| 固定權重集成 | `0.2/0.8`，fusion 之前的做法 | ✅ 保留為對照 |

#### ★ 融合權重：一個支持 Gated Fusion 的實證（v4 新增）

`W_STGCN/W_STGAT = 0.7/0.3` 源自 METR-LA 時期「STGAT 尚未收斂」的權宜設定，該理由已於 STGAT 修復後消失。以**驗證集**掃描 21 組權重（補值全部排除）：

| | 175 節點版 | **202 節點版** |
|---|---|---|
| MAE 最佳權重 | 0.20 / 0.80 | **0.20 / 0.80** |
| RMSE 最佳權重 | 0.70 | **0.65** |
| 集成勝過單獨 STGAT | 0.43% | **0.40%** |
| **兩模型誤差相關係數** | **0.934** | **0.934** |
| 單模型 MAE（val） | STGCN 3.4113 / STGAT 3.2546 | STGCN 3.3585 / STGAT 3.1983 |

**三個發現，且在兩份獨立路段集合、兩批獨立訓練的模型上重現**：

1. **固定權重集成幾乎沒有效益**——最佳集成只勝過單獨 STGAT 0.40%，落在雜訊範圍內。
2. **兩模型錯在同樣的地方**（誤差相關 0.934）。集成獲益的前提是誤差不相關。
3. 🔴 **MAE 最佳權重（0.20）與 RMSE 最佳權重（0.65）落在光譜兩端**——STGCN 誤差變異較小（少犯大錯），STGAT 典型誤差較小（平均較準）。**單一全域常數必須在「平均準」與「不犯大錯」之間二選一。**

> **這正是計畫書 §4.3 Gated Fusion（eq.3）的實證動機**：逐節點、逐時刻的門控能做到常數做不到的事。**負面結果轉為支持原設計的論據。**
> $$\text{Fusion}(s,t)=\tanh\big(W_2 \cdot t \cdot \text{gate} + (I-\text{gate}) \cdot W_3 \cdot s\big),\quad \text{gate}=\sigma(W_1(s+t))$$
> 實作策略：先快取兩模型在全資料集的預測，再訓練輕量 fusion head（成本低、不需重訓主模型）。

**固定權重定案為 `0.2/0.8`，並已在測試集確認（v5 新增）**：

| | val 上挑選 | **test 上確認** |
|---|---|---|
| MAE 最佳權重 | 0.20 | **0.15**（MAE 3.6202 vs 0.20 的 3.6228，**差 0.07%**） |
| 集成勝過單獨 STGAT | 0.40% | **0.29%** |
| 誤差相關 | 0.934 | **0.908** |

**權重沒有過擬合驗證集**，而且 MAE 與 RMSE 的最佳點**第三次**落在光譜兩端
（175 val、202 val、202 test）。§18 掛了很久的「尚未在測試集確認」至此結案。

⚠️ **`0.2/0.8` 使融合結果 ≈ STGAT，因此決策層基準 ④（hybrid）與 ③（STGAT）幾乎相同**。
已驗證**不是程式錯誤**：hybrid 重建的最大誤差 5e-6、corr(hybrid, stgat)=0.9991、
corr(stgcn, stgat)=0.9712、兩者中位相對差 0.62%。這是權重的必然結果。
**Gated Fusion 打破了這個簡併**（§3.7、§4.7 ⑤）。

### 3.3 指標與 mask 評估協定

**MAE / RMSE / MAPE**，分別報告 **horizon 3（15 分）/ 6（30 分）/ 12（60 分）** 與 12 步平均。

**🔴 台中資料一律只在 `mask=True`（真實觀測）的位置計分**：

```python
mask = np.load("data/taichung/mask.npy")   # (T, N) 布林；True = 真實觀測
valid = mask[target_slice]                  # 對齊被預測的時間窗
mae = np.abs(pred - true)[valid].mean()     # 補值位置完全不計分
```

| 資料集 | 遮罩機制 | 原因 |
|---|---|---|
| METR-LA | loss 的 `null_val=0.0`（缺失以 `0` 表示且保留原值） | 見 `實驗記錄` §5.3 Bug 1 |
| **台中 TDX** | **外部 `mask.npy`**（缺失已被 ffill 填補，無 `0` 可遮罩） | 見 §2.3 |

**兩者不可混用**。若在台中資料上沿用 `null_val=0.0`，遮罩會完全失效（資料裡沒有 0），約 25% 的補值會被當成真實觀測計分——這正是 STGAT Bug 1 的翻版。

> 🔴 **評估工具本身也需要守門**：`evaluate_masked.py` 曾在**未帶 `--n-pred`** 的情況下拿 p6／p12 的
> checkpoint 去跑 15 分鐘時程而不報錯（線索是「不同 checkpoint 的 persistence 完全相同」）。
> 已加入「檔名 `_p<N>.pt` 必須與 `--n-pred` 一致」的硬檢查。

**報告時必須同時揭露**：補值比例、以及「指標僅在真實觀測上計算」。否則台中的 MAE 與 METR-LA／論文不可比。

### 3.4 A1 結果（METR-LA，STGAT）

| Horizon | MAE | MAPE | RMSE |
|---|---|---|---|
| 3（15 min） | 2.3101 | 0.0573 | 4.0183 |
| 6（30 min） | 3.2515 | 0.0931 | 6.6575 |
| 12（60 min） | 3.6295 | 0.1084 | 7.5694 |
| **平均** | **3.1827** | 0.0906 | 6.4294 |

參考：persistence 4.622；STGAT 論文 60 分鐘 3.69；計畫書 §5 目標 3.4–3.6。**已達標。**

### 3.5 ★ A2 結果（台中 202 節點版，masked，v4 新增）

**僅計真實觀測（73.8%）**。STGCN 每個時程各訓一次，STGAT 早停於 epoch 19（8,038 秒）。

| 時程 | **Fusion** | STGAT | STGCN | HA | persistence |
|---|---:|---:|---:|---:|---:|
| 15 min | **3.3786** | 3.3802 | 3.5560 | 4.0486 | 4.2872 |
| 30 min | **3.4799** | 3.5127 | 3.7535 | 4.0489 | 4.6744 |
| 60 min | **3.5579** | 3.6276 | 3.9549 | 4.0496 | 5.1281 |
| 12 步平均 | — | 3.4698 | — | — | 4.7328 |

（Fusion 見 §3.7、HA 見 §3.6。以下的「STGAT 領先」是兩個**單模型**之間的比較。）

| 時程 | STGAT vs STGCN |
|---|---:|
| 15 min | **−4.9%** |
| 30 min | **−6.4%** |
| 60 min | **−8.3%** |

相對 persistence：STGAT **−21.1% / −24.8% / −29.2%**；STGCN **−17.1% / −19.7% / −22.9%**。
六項判定全數 PASS（皆優於 persistence、MAE 隨時程單調遞增）。

**三項可寫入報告的發現**：

| # | 發現 | 強度 |
|---|---|---|
| ① | **STGAT 的領先隨時程拉大**（−4.9 → −8.3%），符合其論文「擅長長期預測」的主張 | ✅ **可複製**：175 版為 −4.3/−6.6/−8.6%，換了 27 個路段與整個鄰接矩陣後型態不變 |
| ② | 兩支獨立評估工具的 persistence 吻合到 0.1% 以內 | ✅ 評估流程交叉驗證 |
| ③ | 🔴 **絕對 MAE 的進步來自資料變容易，不是模型變好** | 模型/persistence 的比值在兩版之間全部落在 1% 以內；persistence 本身也降了 2.4%（4.8578 → 4.7328） |

> **報告寫法**：以「相對 persistence 改善 −21 ~ −29%」陳述，**不要寫「MAE 進步 2.2%」**，
> 後者會被 persistence 的同步下降推翻。
>
> 🔴 **兩模型輸入不對等**（STGAT 有 time-of-day 通道，STGCN 沒有）。
> v4 只把這寫成「差距不能純歸因於架構」；**v5 發現它比那更嚴重**——
> 計畫書指派 STGCN「規則性（尖峰時段、星期週期）」，而週期性正是 time-of-day 定義的，
> **它在結構上做不到那件事**（§3.6 ③、§3.7 ④）。
> ⚠️ **先前擔心「補值率 23.7% → 24.6% 會使 MAE 變差」並未發生**——多了 27 個路段、
> 決策層種子邊 +15%，預測品質沒有付出代價。

### 3.6 ★ HA 基準與三個關於資料性質的發現（v5 新增）

計畫書 §4.6 明文要求「與 STGCN、STGAT、**HA（Historical Average）** 比較」。
`integration/ha_baseline.py`（新增）**不需 checkpoint、不需 GPU、不需推論**——純從資料算：

$$\text{HA}(i, r) = \text{mean}\{\, v_{i,r'} : r' \in \text{train},\ \text{dow}(r')=\text{dow}(r),\ \text{tod}(r')=\text{tod}(r),\ \text{mask}_{i,r'}=\text{True} \,\}$$

7 × 288 時槽 × 202 路段，**只用 train 的真實觀測**。每個有值的格平均 14.2 個觀測；
6.98% 的格在 train 沒有任何真實觀測，回退到該路段的 train 平均。

**兩個容易靜默出錯的地方**：

| | 陷阱 | 處置 |
|---|---|---|
| **時區** | 時間戳是 UTC。重貼時槽標籤不改變 MAE，但**星期的邊界會偏 8 小時**，把週一凌晨併進週日——而尖峰正是平日現象 | 全程以 UTC+8 計算 |
| **視窗** | 分數要能與 STGCN/STGAT 並排，就必須落在**完全相同的目標列**上 | 內建 persistence 交叉驗證：算出來必須與 `evaluate_masked.py` 吻合，否則 `exit 1` |

**交叉驗證三個時程全部 `+0.00%`**（4.2872 / 4.6744 / 5.1281）。

#### ① 🔴 模型的「領先幅度」方向會隨對照基準翻轉

```
相對 persistence   −21.1% / −24.8% / −29.2%   ← 領先隨時程「擴大」
相對 HA            −16.5% / −13.2% / −10.4%   ← 領先隨時程「縮小」
```

兩個都是真的。但**只引用 persistence 那組會高估長時程的優勢**——那主要是 persistence
在變爛（4.29 → 5.13），不是模型在變好。HA 與時程幾乎無關（4.0486 → 4.0496）。

> **報告寫法**：**三個時程都必須同時列 HA 與 persistence。**
> 兩個基準的失效方式相反——persistence 知道當下卻不知道結構，HA 知道週間結構卻不知道今天。
> **只贏其中一個是弱證據**；兩個都贏，才證明模型同時用了「當下觀測」與「學到的時空結構」。

#### ② HA 在三個時程都贏 persistence，包括 15 分鐘

與一般交通預測文獻的直覺相反。原因是**本資料集的補值**：persistence 的輸入
`vel[rows − n_pred]` **那一格本身很可能是 ffill 補值**（整體 24.6%）。
旁證：HA 的 unmasked MAE 5.31 遠差於 masked 4.05——補值格對 HA 特別難。

#### ③ 🔴 尖峰是最「規則」的時段，而 STGCN 在那裡輸給 HA

| MAE | 15 min | 30 min | 60 min |
|---|---:|---:|---:|
| HA @ **weekday peak**（07-09、17-19） | **3.6023** | **3.5720** | **3.5054** |
| HA @ weekday off-peak | 3.9709 | 3.9416 | 3.9321 |
| HA @ weekend | 4.1537 | 4.1174 | 4.0927 |
| HA 整體 | 4.0486 | 4.0489 | 4.0496 |
| **STGCN @ weekday peak（60 min）** | — | — | **3.5904** ← **輸給 HA** |

異常度分桶（定義在**輸入視窗**上，非 `|truth − HA|`，故不循環），60 分鐘：

| 分桶 | HA | STGCN | STGAT | Fusion |
|---|---:|---:|---:|---:|
| Q1 most routine | 1.6674 | **1.7568 ← 輸** | 1.6015 | **1.5328** |
| Q2 | 2.4533 | **2.5181 ← 輸** | 2.3105 | **2.2446** |
| Q3 | 3.6379 | 3.6363 | 3.3363 | **3.2644** |
| Q4 most unusual | 7.9150 | **7.2665 ← 贏** | **6.6047** | 6.6268 |

**計畫書指派 STGCN「規則性」，但它在最規則的情境下打不贏一個連模型都不是的週期平均。**
根因是它的輸入沒有 time-of-day 通道（`[B,1,12,N]` vs STGAT 的 `[B,N,12,2]`）——
**一個模型無法表示它看不到的東西。**

#### ④ ★ 但 STGAT 那一半成立：優勢隨異常度單調放大

STGAT 相對 HA：Q1 −4.0% → Q4 **−16.8%**。這是計畫書 §4.3「STGAT path 捕捉異常性
相依」第一次有**直接的量化支持**，而且用的是一個不需訓練任何東西的基準。

---

### 3.7 ★★ Gated Fusion：計畫書 §4.3 的雙路架構（v5 新增）

```
gate   = σ( W₁ · (s + t) )
Fusion = tanh( W₂ · t · gate + (I − gate) · W₃ · s )
output = FC(Fusion)  →  15 / 30 / 60 分鐘
```

`s`、`t` 是兩條 path 的**隱藏特徵**——計畫書把融合放在骨幹與輸出頭之間
（若 s、t 已是最終速度，後面那層 FC 就沒有存在的理由）。

#### ① 三層落差：兩條管線沒有一處相同

| | STGCN | STGAT |
|---|---|---|
| **視窗數（test, p12）** | **7,518** | **7,539** |
| 輸入形狀 | `[B, 1, 12, N]` | `[B, N, 12, 2]` |
| 第二通道 | 無 | **time-in-day**（UTC+8 的日內比例） |
| **正規化** | **逐路段** z-score | **單一全域純量** z-score |
| 輸出 | 單步 | 十二步 |

**但視窗定義其實相同**：令 `g = i = t−11`，兩者都是「輸入 `g..g+11`，時程 p 的目標在
`g+11+p`」。視窗數不同只是修剪切分邊界的方式不同。
**正規化則不能統一**——拿全域尺度的輸入餵逐路段尺度訓練的 STGCN，**不報錯，只輸出垃圾**。

#### ② 實作定案：聯合訓練 + 補 STGCN 的 time-of-day

`fusion/`（新增，1,241 行）以**別名**把兩個 `model/` 套件載進同一 process
（兩個 repo 都有同名套件），並提供一份統一的 dataloader，從同一批列產生**兩個**張量。

| | 凍結骨幹 | 聯合訓練（**採用**） |
|---|---|---|
| 能否突破誤差相關 0.934 | ❌ 兩個凍結的骨幹錯在同樣的地方 | ✅ gate 給了兩條 path 分工的理由 |
| 一個模型三時程 | ❌ STGCN 骨幹綁定單一時程 | ✅ 新的 FC 頭直接吐 12 步 |

⚠️ **補 time-of-day 不算偏離計畫書**——§4.3 白紙黑字要求 STGCN Path 萃取
「尖峰時段、星期週期」，而那正是 time-of-day 定義的東西。現有實作是漏了。

#### ③ 🔴 管線驗收：先證明等價，再訓練

`fusion/verify.py` 把**已訓練好的**兩個骨幹推過新 dataloader，檢查能否重現各自的 MAE。
切視窗、兩套正規化、張量排列與遮罩其中任一項寫錯都**不會報錯**——訓練照跑、loss 照降。

```
                    15 min     30 min     60 min
  stgat             3.3833     3.5160     3.6308
  reference         3.3802     3.5127     3.6276   ok ok ok
  stgcn             3.5577     3.7537     3.9549
  reference         3.5560     3.7535     3.9549   ok ok ok
  persistence       4.2886     4.6755     5.1281
  reference         4.2872     4.6744     5.1281   ok ok ok
```

**九項全在 0.1% 內**（這是第四支獨立工具與既有評估流程對上）。
順帶抓到上游 `data_transform` 的 off-by-one：**每個切分的最後一列從來沒有被預測過**。

#### ④ ★★ 結果：計畫書的雙路前提，有一半原本不成立

| | 15 min | 30 min | 60 min |
|---|---:|---:|---:|
| **Fusion** | **3.3786** | **3.4799** | **3.5579** |
| STGAT | 3.3802 | 3.5127 | 3.6276 |
| **vs STGAT** | **−0.0%** | **−0.9%** | **−1.9%** |

**依事先寫下的判準（改善 < 1% 報告為「無可測量的效益」），只有 60 分鐘的 −1.9% 站得住。**

⚠️ val 上曾勝固定權重 2.52%，**test 只剩 1.9%**——早停選中的 epoch 恰好是 val 曲線的
最低點。**用某個切分做選擇，就不能用那個切分報告效果。**

**閘門確實在動,而且自己學到了固定權重的最佳值**：

```
mean opening                  0.8304    ← §13.7 三次搜尋都落在 0.80–0.85
spread ACROSS SECTIONS        0.0350
spread ACROSS TIME            0.0515    ← 時間上的變動是空間上的 1.5 倍
```

**但改善的來源不是閘控**（§3.6 ③ 的分桶表）：優勢集中在**規則**那一端、到 Q4 消失，
與「更好的閘控」的預期方向**相反**，而與「給 STGCN 時間通道」完全一致。

| 來源 | 貢獻 |
|---|---|
| **STGCN 補 time-of-day** | **主要** |
| 端到端重訓（單一 loss、多時程目標） | 次要 |
| 逐節點逐時刻閘控 | 在動，但幅度小（±0.05 搬不動 4.3% 的誤差） |

> **可寫入報告的結論**：
> **「計畫書的架構前提有一半原本不成立——補上一個它自己就要求的輸入特徵之後才成立。」**
> 異常性那一半（STGAT）本來就成立，且 fusion 在 Q4 **超不過 STGAT**——那一端已被佔滿。

⚠️ **待補的消融**：不帶 `--stgcn-tod` 重跑一次，`C − C₀` 才是 time-of-day 貢獻的直接量測。
目前的歸因是**推論**，不是實測。


---

## 4. 軌道 B：決策實驗

### 4.1 ★ 實驗場域建構（v4 全面改寫）

v3 寫的是「裁切至 200–300 節點」。實作後兩點與 v3 的假設不同：

1. **合併 degree-2 節點的效益比預估更大**：實測 39,920 → **9,904 節點（4.0 倍）**，v3 估的是 2–3 倍。
2. **200–300 節點做不到，也不該做**：路網必須**強連通且提供替代道路**才有分流可言，實測下限約 **1,690 條邊 / 840 節點**。再縮小已實測失敗（見 §5）。

#### `build_arena.py` 的四個步驟

```
① 主幹道種子：lanes >= 3 的邊 + 全部 601 條 TDX 種子邊     ← 有預測訊號的骨幹
② 走廊補齊  ：加入 134 條邊，使種子連成連續走廊             ← 含臺灣大道（供 S3 用）
③ 連通補齊  ：加入 1,154 條邊，保證強連通（3 輪）           ← 羊群才有地方分流
④ ★ 再簡化  ：合併切邊後新產生的中途節點                    ← v4 新增，見下
⑤ 端點檢查  ：東海大學（snap 107.9 m）、台中車站（snap 86.9 m）皆在場域內
```

#### ★ 步驟 ④：為什麼取子圖之後必須再簡化一次

取子圖會把原本的路口變回「一進一出」的中途節點（實測 684 / 1,442 = 47.4%）。後果是**逐節點決策的 policy 在超過半數的決策點沒有選擇**：

```
候選數 1: 2,353 (54.5%)   ← 動作被迫，log π = 0，policy 梯度恆為零
候選數 2: 1,718 (39.8%)
候選數 3:   249 ( 5.8%)
```

再簡化的結果：

| | 合併前 | **合併後（定案）** |
|---|---:|---:|
| 節點 / 有向邊 | 1,442 / 2,737 | **840 / 1,690** |
| 平均路徑 | 46.8 跳 | **28.7 跳（−39%）** |
| 分岔點比例 | 77% | **79%** |
| **覆蓋率（按邊數）** | 15.2% | **24.6%** |
| **覆蓋率（按長度）** | 14.1% | **14.1%** ← 完全不變 |
| 種子邊 | 416 | **416（全部保留）** |
| TDX 路段代表性 | 141 / 202 | **141 / 202** |

**兩條保護規則**（合併邏輯重用 `build_simplified_network.py` 的 `step` / `aggregate`，但兩類東西不能合併，共同原因是**外部有程式用 id 直接定址它們**）：

| 保護對象 | 為什麼 | 代價 |
|---|---|---|
| 含**種子邊**的鏈 | `section_to_edges.csv` 用 `(u, v)` 掛預測，合併會**靜默切斷 TDX 訊號**；且「一半有預測」的邊 `make_drl_input` 表達不了 | 684 個中途節點中保留 81 個 |
| **Demo 端點** | Demo 用 node id 指定起訖 | 1–2 個節點 |

> ⚠️ 第二條是漏掉後才補的——首次執行時**台中車站剛好是個中途節點，被合併掉了**。
> 保護了種子邊卻忘了端點，**同一類疏漏犯了兩次**。

#### 場域規格（`Map/arena_meta.json`，定案值）

| 項目 | 值 |
|---|---|
| 節點 / 有向邊 | **840 / 1,690** |
| 分岔節點 | 663（79%） |
| 平均 / 最長路徑 | **28.7 / 79 跳** |
| 種子邊（全網 601 → 場域內） | **416** |
| **邊側覆蓋率（按邊數）** | **24.6%** |
| **邊側覆蓋率（按長度）** | **14.1%** ← §5 說明為何這個才是有效的 |
| TDX 路段代表性 | 141 / 202 |
| Demo 端點 | 東海大學、台中車站皆在場域內 |

> **節點 id 沿用原始 OSM id**，故場域路徑可直接畫在完整地圖上。
> 🔴 **Windows 陷阱**：`astype(int)` 在 Windows 為 int32，而 **84.7% 的 OSM id 超過 2³¹−1**。
> 一律用 `to_numpy(dtype="int64")`，並加行數斷言。

### 4.2 邊屬性

| 屬性 | 來源 |
|---|---|
| `length` | OSM `length_m`（公尺） |
| `t0`（自由流時間，秒） | `length_m / (速限 × 1000/3600)` |
| `tpred`（預測時間，秒） | 由軌道 A 的預測經 map-matching 提供（見 §5）；無覆蓋則依 `TAICHUNG_TPRED_FALLBACK` |
| `cap`（容量） | `lanes × 1,360 veh/h`，再乘 `capacity_scale` 校準（見 §4.4） |
| `road_name` / `district` | ✅ `Map_fined` 已提供（S3 需要，見 §4.3） |

#### ✅ 速限缺失問題已解決（v3 為 93.7% 缺失）

v3 時 `free_flow_speed_kmh` 有 **93.7% 為空**，且已驗證**用 `lanes` 補值行不通**（1/2/4/5/6 車道的速限中位數全為 50，僅 3 車道為 60，幾乎沒有鑑別力）。

**v4：組員 A 已於 `Map_fined` 依《道路交通安全規則》§93 補上 50/30 兩級，並附 `speed_imputed` 旗標**（補值率 88.2%，但**有依據且可追蹤**）。`config.TAICHUNG_DEFAULT_SPEED_KMH = 50.0` 保留為最後防線。

> **為何這件事重要**：`t0` 是 **DRL 獎勵函數的基底**（`_gcost` 以 `t0` 走 BPR），也是 `static` 基準的成本來源。
> 羊群抑制的核心問題是「主幹道塞住時，值不值得繞去平行小路」，這**取決於主幹道與小路的速度比**。
> 若小路實際為 40 km/h 卻一律設為 30，**繞道會顯得比實際更不划算，系統性低估分流意願**。
>
> **建議的敏感度檢查（非阻擋項）**：預設值分別以 30 / 40 / 50 km/h 各跑一次；
> 若 Gini 改善的方向與幅度一致，即可聲明「結果對預設速限不敏感」。

### 4.3 需求情境

| # | 情境 | OD 產生方式 | 目的 | 狀態 |
|---|---|---|---|---|
| **S1** | 正常負載 | 起訖點皆隨機 | 對照組：低壅塞下方法**不應**造成傷害 | ✅ `SCENARIO=random` |
| **S2** | 尖峰漏斗 | 終點集中於 `N_HOTSPOTS=4` 個高 in-degree 樞紐 | **主場景**：羊群效應顯現處 | ✅ 主結果（§4.7） |
| **S3** | 主幹道封閉 | S2 基礎上，**派遣進行中**移除一條主幹道 | **計畫書開篇的動機情境** | ✅ **完成**（設計見下、結果見 §4.7） |

#### S3 的三個設計決定（v5 定案）

`integration/closure.py`（新增）。**封閉是遮蔽，不是從圖上移除**——actor 的
`edge_index` / `edge_static` 是會進 `state_dict` 的 buffer，少 83 條邊就載不進 checkpoint；
而且遮蔽在物理上也對：**第一波車留在封閉路段上的負載照算**。

| # | 決定 | 定案 | 理由（皆為實測） |
|---|---|---|---|
| 1 | **關哪條路** | **臺灣大道全線**（83 邊、20.6 km） | `--close-busiest` 會選到**松竹路**，而關掉它會**切斷 4 個 hub**，269/799 台車無路可走。**最忙的路正好是最不能關的那條** |
| 2 | **關的範圍** | 整條命名道路（`--close-road` 用**前綴**比對，故 `臺灣大道三段` 也能單獨關） | 見下方「負載佔比預測不了衝擊」 |
| 3 | **關的時機** | **`--close-at 0.10`**（10% 的車已派遣） | 只有中途封閉才重現「所有人同時拿到同一條新的最快路」。時機的影響**高度非線性**，見下 |

#### 🔴 量測一：負載佔比完全預測不了衝擊

同一批 766 台車、同一組參考邊，**從頭封閉**（衝擊上界）：

| 封閉 | 邊數 | 基準負載佔比 | ATT | ΔATT |
|---|---:|---:|---:|---:|
| （未封閉） | 0 | 0.00% | 723.3 | +0.0% |
| **臺灣大道（整條）** | 83 | 8.81% | 1479.8 | **+104.6%** |
| 臺灣大道二段 | 36 | **6.32%** | 757.3 | **+4.7%** |
| **臺灣大道三段** | 20 | **2.24%** | 1467.7 | **+102.9%** |
| 臺灣大道四段 | 5 | 0.00% | 723.3 | +0.0% |

**最忙的二段關掉只 +4.7%；最不忙的三段之一卻造成 +102.9%。**
三段是咽喉，二段有好的平行道路。

> **教訓**：「這條路多重要」不能用它承載多少流量判斷，要看**移除它之後替代路徑貴多少**。
> 與 §5「按長度的覆蓋率才是有效的那個」同型——用一個看似合理的代理量去排序而排錯。

#### 🔴 量測二：`--close-at` 高度非線性，且 Gini 的方向會反轉

| `--close-at` | ΔATT | ΔGini |
|---|---:|---:|
| 0.00 | **+104.6%** | **+5.9%** |
| 0.10 | +90.5% | +3.7% |
| 0.25 | +50.3% | +1.0% |
| 0.50 | +17.1% | **−2.3%** |
| 0.75 | +2.2% | **−2.5%** |

ATT 掉這麼快是 BPR 的四次方項——替代道路上的車少一半，超額延誤就少約 2⁴ = 16 倍。

🔴 **但 Gini 在 0.5 之後反而下降**：封閉把車流切成兩組不同的路線集合，
負載攤在更多邊上，**比大家都走同一條路還平均**——封閉本身當了一次粗糙的負載平衡器。

> **這修正了 v4 的期待。** v4 §10 曾寫「S3 是唯一可能讓 Gini 達標的路」。
> **只有早期封閉才會拉高基準 Gini**，而 §4.7 的實測進一步顯示：即使 `--close-at 0.10`，
> **oracle 自己的 Gini 改善反而略差**（−18.1% → −17.4%）。**S3 放大 ATT，救不了 Gini。**

#### 🔴 量測三：封閉後的斷裂是「場域裁切的假象」

```
場域 840 節點   → 關臺灣大道 → 最大 SCC 780（92.9%），60 個節點掉出去
完整路網 9,904 → 關同一條路 → 最大 SCC 9,823（99.2%）

★ 掉出去的 60 個節點裡，57 個（95%）在完整路網上仍然連通
```

若把那 3.8%±0.5% 的行程計為「封閉造成的未送達」，等於**把 `build_arena.py` 的裁切決策
當成研究發現**，而且會讓 ⑦ 掉到 95% 門檻以下、整列被標 `NOT COMPARABLE`。

**定案：`--close-demand filter`**——沿用 S2 **完全相同**的需求，只排除封閉後不可行的行程。
4 個 hub 跨 10 seeds 相同且全部存活。**報告須揭露這個排除與它的依據。**

> **為何整批過濾而非分波過濾**：整批過濾讓需求**與 `--close-at` 脫鉤**，
> 封閉時機的敏感度分析才能配對比較。

### 4.4 壅塞模型與容量校準### 4.4 壅塞模型與容量校準

**BPR volume-delay**（SUMO 接入前的代理）：
$$t_e(\text{load}) = t_0\left(1 + 0.15\left(\frac{\text{load}}{\text{cap}}\right)^{4}\right)$$

**容量校準**：CSV 容量為 veh/h（1,360–8,158），與模擬的車輛數尺度不符——直接使用會導致 ρ≈0、完全不壅塞、無羊群可抑制。以 `calibrate_taichung.py` 掃描，選定 `(n_vehicles, capacity_scale)` 使**羊群基準的 worst-ρ 落在 2–3.5**（確保有壅塞可抑制，但未完全癱瘓）。

| 場域版本 | `capacity_scale` | 800 車下的羊群基準 worst-ρ |
|---|---|---|
| 完整路網（20,390 邊） | 0.0400 | ~3.0 |
| 1,224 節點場域 | 0.0441 | — |
| 1,442 節點場域 | 0.0414 | 3.46 |
| **840 節點場域（定案）** | **0.0429** | **3.33 ± 0.27（10 seeds）** |

> ⚠️ **車輛數改變就必須重跑校準**。0.0429 只對應 800 車。

### 4.5 七個對照策略（完整消融階梯）

| # | 策略 | 用預測？ | 協調？ | eq.4？ | 對應計畫書 |
|---|---|:--:|:--:|:--:|---|
| ① | `static`（自由流 Dijkstra） | ✗ | ✗ | ✗ | 基準 ① |
| ② | `stgcn + Dijkstra` | ✓（純 STGCN） | ✗ | ✗ | 基準 ② |
| ③ | `stgat + Dijkstra` | ✓（純 STGAT） | ✗ | ✗ | 基準 ③ |
| ④ | `hybrid + Dijkstra` | ✓（融合） | ✗ | ✗ | **羊群基準**（Δ 的分母） |
| ⑤ | `load-aware` | ✓ | ✓ | ✗ | 消融：隔離「協調」的貢獻 |
| ⑥ | `global-penalty`（oracle） | ✓ | ✓ | ✓ | **性能上界／學習目標** |
| ⑦ | **`DRL`（PPO + Residual E-GAT）** | ✓ | ✓ | ✓ | **本系統**（計畫書 基準 ④） |

**設計說明**：
- **④ 為 Δ 的比較基準**——它代表「所有車都看同一份預測、各自貪婪選最快路」，這正是羊群效應的定義。
  **v5 起 ④ 吃的是 gated fusion**（§3.7），與計畫書所述的架構一致。
- **⑥ 是 oracle，不是本系統**。它使用全域 Dijkstra + 精確全域負載 + 每批重算全圖，是 DRL 刻意不擁有的能力。報告中須明確定位為「**eq.4 目標的實用上界**」。
- ✅ **③④ 的簡併已被 fusion 打破**（v4 是 `+0.0±0.0%`，即路徑逐條相同；v5 是 `+0.8±0.7%`）。
  §4.8 曾預測「要讓 ③④ 分開，得先讓兩個模型的預測真正不同」——應驗了。

#### ★ ⑦ 一律報告兩列：貪婪解碼與 beam-8 解碼（v5 新增）

**同一組權重、同一份觀測，只有解碼方式不同**，所以每一條邊仍由 πθ 提出並評分——
這是**解碼方式的消融，不是換一個決策者**（基底論文 Lei et al. 2022 的比較表本就並列 `G / BS`）。

- 展開依**累積 log 機率**（policy 自己的偏好），最終在已完成的候選中依**實際 eq.4 成本**挑選。
  若連搜尋過程都用成本排序，就變成成本導引的搜尋，而非「把 policy 讀得更寬」。
- `test_beam.py`（新增）驗證**寬度 1 逐節點等於貪婪**——這是任何 beam 數字可信的前提。

**Beam 修好的是完整性，不是路由品質**：dead-end S2 36.8 → 0.5、S3 132.4 → 29.6，
但無封閉時加寬 4 倍只讓 ATT 動 0.8%，且 10 seeds 的 headline **變差**（−38.4% → −36.0%），
因為救回來的是昂貴的行程。

**待決定的第八列**：計畫書要求輸出 15/30/60 三個時程。fusion 完成後一次 forward 就有三個時程，
**⑦a（單時程）vs ⑦b（三時程觀測）的消融終於可行**，但會多一次完整 DRL 訓練。

### 4.6 指標（計畫書 §4.6）

| 指標 | 定義 | 方向 |
|---|---|---|
| **Served %** | 成功抵達比例 | ↑ |
| **ATT** | 所有車輛平均通行時間 | ↓ |
| TSTT | 系統總通行時間 | ↓ |
| **Gini(邊負載)** | 邊負載分佈的 Gini 係數——**量化羊群抑制程度的核心指標** | ↓ |
| **worst-link ρ** | 最壅塞路段飽和度 | ↓ |
| Throughput | 單位時間完成行程的車輛數（代理：served / ATT） | ↑ |

**評分規則**：所有策略的路徑產出後，**一律在同一個 BPR 模型下重新評分**；Gini 以「所有策略用過的邊」聯集為參考集合（確保可比）。

#### 🔴 三項守門（皆針對「不報錯的錯」）

| # | 守門 | 防什麼 |
|---|---|---|
| 1 | **`.meta.json` sidecar** | 任何「會改變模型輸入或 policy 觀測」的設定，存檔時一併寫出、讀檔時自動套用並比對。已用於預測輸出（`taichung_pred_*.meta.json`）與 DRL checkpoint（`togo_refresh` / `reward_scale` / 各 mult / `capacity_scale`）。**訓練與評估設定漂移不會報錯，只會靜默讓結果變差。** |
| 2 | **`served%` 為第一欄 + `MIN_SERVED=0.95` 標記** | served 不同的策略，ATT 根本不可比（見下）。低於門檻的列印出 `NOT COMPARABLE` |
| 3 | **`congestion_note()`（`CONGESTION_BAND=(1.5, 5.0)`）** | 若羊群基準的 worst-ρ 落在區間外，代表校準失效、整份比較沒有意義 |

#### 🔴🔴 一個放棄困難任務的策略，可以「贏過」理論上界（v5 更新）

不同策略放棄的行程數不同，而**被放棄的行程確實較難**。v4 用共同子集估計，
v5 用 beam search 把行程救回來後**直接量到**：

```
S3   ⑦ 貪婪    ATT −63.0%   (served 82.8%)
     ⑥ oracle  ATT −61.2%   (served 100%)     ← 貪婪「打贏」了理論上界
     ⑦ beam-8  ATT −55.8%   (served 96.0%)    ← 修正後，如預期落後 oracle
```

**灌水幅度隨放棄率放大**：

```
S2   放棄  4.6%  →  ATT Δ 灌水  2.4pp
S3   放棄 17.2%  →  ATT Δ 灌水  7.2pp   ← 足以翻過 oracle
```

> **這是守門 2 存在的最強證據。** 沒有它，報告會出現「本系統優於理論上界」這種
> **必然錯誤**的結論，而表格本身不會有任何異狀。
>
> **報告寫法：⑦ 一律引用 beam-8 那一列**，貪婪列為消融並註明其 served%。

⚠️ 注意 `metrics.evaluate()` 會丟掉失敗的行程，導致索引與需求錯位——
`diagnose_agent.py` 因此自備 `realized_times()`，不可直接重用前者做子集比較。
**且多個 agent 必須在同一次執行裡比**：分開跑的共同子集不同，實測兩次差 8 台車就讓
static 的 ATT(common) 移動 3.7%——**比要找的效果還大**。

⚠️ **Gini 以「所有策略用過的邊」為參考集合，所以多加一列策略，每一列的 Gini 都會微幅移動**
（ATT 與 worst-ρ 不受影響）。**跨執行引用 Gini 前，先確認兩次的策略集合相同。**

### 4.7 統計協定與正式結果（v5 更新）

- **10 組隨機 seed × 情境**，每個指標報告 **mean ± std**
- **Δ 採配對（paired）計算**：在**每個 seed 內**先算出相對羊群基準的百分比，再跨 seed 平均。
  因所有策略在同一 seed 看到同一份需求，配對比較可消去需求本身的變異

```bash
cd integration
python run_compare.py --graph taichung --vehicles 800 --repeat 10 \
       --drl checkpoints/taichung/drl_fusion_togo25.pt --beam 8
```

#### ★★ 正式結果（840 節點場域，800 車，10 seeds，⑦ = fusion agent，2026-08-27）

**S2 — 尖峰漏斗（主情境）**

| # | 策略 | served% | ATT | worst-ρ | Gini(load) |
|---|---|---:|---:|---:|---:|
| ① | static（自由流） | 100% | 848.3±161.6 | **3.3302±0.2703** | 0.7180 |
| ② | STGCN + Dijkstra | 100% | 861.2±164.1 | **3.3302±0.2703** | 0.7121 |
| ③ | STGAT + Dijkstra | 100% | 866.5±164.6 | **3.3302±0.2703** | 0.7123 |
| ④ | **hybrid（羊群基準）** | 100% | 860.4±166.9 | **3.3302±0.2703** | 0.7128 |
| ⑤ | load-aware（僅協調） | 100% | 476.4±9.8 | 2.1099±0.0446 | 0.6892 |
| ⑥ | global-penalty（oracle） | 100% | 468.2±11.7 | **1.9282±0.0837** | **0.5838** |
| ⑦ | DRL — 貪婪解碼 | 95.4%±1.8 | 515.4±15.6 | 2.3858±0.0999 | 0.6248 |
| **⑦** | **DRL — beam-8** | **99.9%±0.2** | 539.3±33.8 | 2.5075±0.1781 | 0.6407 |

**配對 Δ vs 羊群基準**

| 策略 | ATT | **Gini** | worst-ρ |
|---|---:|---:|---:|
| ⑤ load-aware | −42.9±9.8% | −3.3±0.5% | −36.2±5.8% |
| ⑥ oracle | −44.0±9.0% | **−18.1±0.7%** | −41.9±2.7% |
| ⑦ DRL — 貪婪 | −38.4±9.7% | −12.3±0.8% | −28.1±3.6% |
| **⑦ DRL — beam-8** | **−36.0±7.9%** | **−10.1±0.6%** | **−24.6±2.0%** |

**S3 — 臺灣大道於派遣 10% 處封閉**

| 策略 | ATT | **Gini** | worst-ρ | served% |
|---|---:|---:|---:|---:|
| ⑤ load-aware | −60.4±4.1% | −7.7±0.5% | −35.7±4.5% | 100% |
| ⑥ oracle | −61.2±4.1% | **−17.4±0.8%** | −43.3±2.2% | 100% |
| ⑦ DRL — 貪婪 | −63.0±4.5% ⚠️ | −13.6±1.0% ⚠️ | −36.1±3.4% ⚠️ | **82.8%±2.8** |
| **⑦ DRL — beam-8** | **−55.8±5.6%** | **−12.9±0.8%** | **−27.5±2.9%** | **96.0%±1.8** |

#### 對照計畫書 §5 的門檻（一律用 beam-8 列）

| 指標 | 目標 | S2 | S3 | 判定 |
|---|---|---:|---:|---|
| ATT（突發負載） | ↓20–30% | **−36.0±7.9%** | **−55.8±5.6%** | ✅ **達標** |
| worst-link ρ | ↓20%+ | **−24.6±2.0%** | **−27.5±2.9%** | ✅ **達標**（v4 曾為 −19.8%，恰在門檻上） |
| Gini(edge load) | ↓30%+ | −10.1±0.6% | −12.9±0.8% | ❌ **未達**（**oracle 亦僅 −18.1 / −17.4%**） |

#### 四項可寫入報告的發現

**① Gini 的穩定性跨資料集重現**：Gini 的跨需求標準差 ±0.6%，ATT 是 ±8%。
**核心主張應建立在 Gini 上**——它的變異只有 ATT 的十分之一。

**② ★ 協調不只降低 ATT，還讓 ATT 變得可預測**

| 策略 | ATT 標準差 | 相對 ① 的變異縮減 |
|---|---:|---:|
| ① static / ④ hybrid | ±161.6 / ±166.9 | — |
| ⑤ load-aware | **±9.8** | **16.4 倍** |
| ⑥ oracle | **±11.7** | 13.9 倍 |
| ⑦ DRL（beam-8） | ±33.8 | 4.9 倍 |

> **羊群效應不只讓平均通行時間變差，還讓它變得不可預測**——同樣的路網、同樣的車輛數，
> ATT 可差 ±19%。對導航服務而言「今天多久到」無法估計，本身就是嚴重的服務品質問題。

**③ ⑦ 在 Gini 上明顯優於 ⑤，但 ATT／worst-ρ 不如**

```
Gini      S2  ⑦ −10.1%  ⑤ −3.3%      S3  ⑦ −12.9%  ⑤ −7.7%     → ⑦ 明顯較佳
ATT       S2  ⑦ −36.0%  ⑤ −42.9%     S3  ⑦ −55.8%  ⑤ −60.4%    → ⑦ 較差
worst-ρ   S2  ⑦ −24.6%  ⑤ −36.2%     S3  ⑦ −27.5%  ⑤ −35.7%    → ⑦ 較差
```

**這是全域懲罰被學到的證據**：agent 學到的是「**分散負載**」而非「單純避開最壅塞的路」，
與 eq.4 的設計意圖一致。**事故情境下這件事的價值更高**——⑦ 捕捉到 oracle 之 Gini 改善的
比例由 **56%（S2）升至 74%（S3）**。

> 🔴 **但不可寫成「用 ATT 換 Gini 的取捨」**。逐 seed 配對顯示：
> **同一組 λ 下，oracle 在 ATT 9/10、Gini 10/10、worst-ρ 10/10 全面優於 load-aware**，
> 而 **DRL 在三個指標上都 0/10 輸給 oracle**。
> 取捨並不存在於方法層級——存在的是 **agent 與 oracle 的能力差距**。
> **因此「調 λ」是在解錯的問題**（§4.8）。

**④ 🔴 ①②③④ 之間的差距，小於它自己的雜訊**

```
S3，① static vs ④ 的 ATT Δ
  固定權重集成   −0.3 ± 0.2%
  gated fusion   −1.7 ± 2.1%     ← 標準差變成 10 倍
```

v4 曾把 `±0.2%` 讀成「四者的塌陷極度穩定」。**那是簡併的假象**——`0.2/0.8` 讓 ④ 的路徑
幾乎逐條等於 ③，配對 Δ 因此幾乎沒有變異。fusion 打破簡併後，真實的變異是 **±2.1%**，
而 ①②③ 相對 ④ 的差距（1.3–1.7%）**小於那個變異**。

> **結論不變，但理由要從「差異極小」修正為「差異小於雜訊」**——後者才誠實。

### 4.8 ★ agent 與 oracle 差距的歸因（v4 新增）

`diagnose_agent.py` 把三個常被混淆的原因分開：**繞路 / 壅塞 / 折損（attrition）**。

| 策略 | served | 繞路比 | p90 | ATT(all) | **ATT(common)** |
|---|---:|---:|---:|---:|---:|
| ① static | 799 | 1.000x | 1.000x | 790.1 | 758.8 |
| ④ hybrid | 799 | 1.012x | 1.035x | 810.8 | 778.3 |
| ⑤ load-aware | 799 | 1.029x | 1.045x | 477.2 | 467.9 |
| ⑥ oracle | 799 | **1.131x** | 1.366x | 464.9 | **456.4** |
| ⑦ DRL | 774 | **1.096x** | 1.278x | 518.4 | **518.4** |

1. **oracle 繞得比 load-aware 更多，ATT 卻更低**——繞遠路本身不是問題。
2. **agent 繞得比 oracle 少，同一批行程卻慢 13.6%**——「過度繞路」的假設**被否證**；症狀是**選進壅塞區**。

**機制**：`_dist_to_dest` 原本以 `weight="t0"` 計算「還有多遠」，而該值餵給候選特徵、A\* 提示 `to_go`、E-GAT 節點特徵與 potential shaping——**agent 看得到下一跳的 ρ，卻看不到那一跳之後的路況**。差別**不在資訊新鮮度**（agent 的 ρ 比 oracle 更新），**而在前瞻**。

**修正 `--togo-refresh K`**：`_dist_to_dest` 改用 `_gcost`（與獎勵同一個成本函數）當權重，每 K 台車重算。快取依目的地，hotspot 僅 4 個 hub → 800 車 K=25 只需 128 次 Dijkstra。

以解析式 agent（`GreedyOracleAgent = argmin(gcost + to_go)`）驗證機制：

```
togo_refresh=0   (自由流 to-go)     served  94/799   1.01s
togo_refresh=25  (eq.4 成本 to-go)  served 575/799   1.43s      ← 送達率 6 倍，成本 +42%
```

⚠️ `togo_refresh` **改變 policy 讀到的觀測，不能只在評估時開啟**——必須重訓，由 sidecar（§4.6 守門 1）強制。

#### ⑤ 重訓結果：機制確認，但確認的是「繞路的量」（v5 新增）

同一批 741 台車、同一次執行（跨 agent 比較必須如此，見 §4.6）：

| 策略 | 繞路比 | p90 | ATT(common) | **離 oracle** |
|---|---:|---:|---:|---:|
| ④ 羊群基準 | 1.012x | 1.035x | 773.4 | 72.7% |
| ⑤ load-aware | 1.029x | 1.044x | **460.0** | **2.7%** |
| ⑥ oracle | **1.128x** | **1.360x** | 448.0 | 0.0% |
| ⑦ 舊 agent（`togo_refresh=0`） | 1.095x | 1.279x | 510.9 | 14.1% |
| **⑦ togo25** | **1.127x** | **1.361x** | **499.6** | **11.5%** |

**判準通過**：ATT(common) 510.9 → 499.6，**縮小 18% 的 oracle 差距**。
10 seeds 上 worst-ρ 也因此由 −19.8% 推到 −27.2%，**跨過計畫書門檻**。

#### ⑥ ★★ 但最尖銳的發現是：繞路的「量」對上了，「質」沒有

```
繞路比   togo25 1.127x  ≈  oracle 1.128x     ← 逐位相同
p90      togo25 1.361x  ≈  oracle 1.360x     ← 逐位相同
ATT      togo25  499.6  vs  oracle  448.0    ← 仍差 11.5%
```

更關鍵的對照：**⑤ load-aware 只繞 1.029x，ATT 卻是 460.0——比繞 1.127x 的 agent 還好。**

> **繞得跟 oracle 一樣多是不夠的，要繞到對的地方。**
> to-go 的修正給了 agent 正確的「繞多少」，但沒有給它「繞去哪」。

**這把剩餘差距定位得很精確**——不是 λ 權重（§4.7 ③ 已用 oracle 否證）、
不是繞路幅度（現在逐位對上）、而是**在給定的繞路預算下選哪一條路徑**。

#### ⑦ beam search：v4 的暫緩理由失效，但結論仍是「修不好品質」

v4 曾以「指南針偏了，加寬搜尋只會找到更多同樣偏差的路徑」暫緩 beam search。
**指南針對準之後那個理由不成立**，所以 v5 實作並量測了它：

- **修好完整性**：dead-end S2 36.8 → 0.5、S3 132.4 → 29.6，⑦ 在兩個情境下都完全可比
- 🔴 **修不好品質**：無封閉時加寬 4 倍只讓 ATT 動 0.8%；10 seeds 的 headline **變差**
  （−38.4% → −36.0%），因為救回來的是昂貴的行程

> **所以離 oracle 的 11.5% 是模型誤差，不是搜尋誤差。** v4 的推測被**否證**。
> beam search 買到的是**完整性**（也就是誠實的數字），不是路由品質。

---

## 5. 耦合：預測 → 決策

```
202 個 TDX section 的預測速度
        │  一對多 map-matching（section_to_edges.csv）
        ▼
場域的邊
  ├─ 有 TDX 覆蓋（416 / 1,690）→ tpred = length / 預測速度
  └─ 無 TDX 覆蓋（1,274 / 1,690）→ 依 TAICHUNG_TPRED_FALLBACK
```

**map-matching 品質**：

| 指標 | v3（舊地圖） | **v4（`Map_fined`）** |
|---|---|---|
| TDX 路段總數 | 244 | 244 |
| 通過 300 m 門檻 | 212（86.9%） | **244（100%）** |
| 一對多路徑還原成功 | 209 / 212 | **237 / 244**（7 個退回單一邊） |
| 種子邊（全部路段） | 621 | **715** |
| 種子邊（存活的 202 路段） | 524 | **601** |

### 🔴 核心負面結果：預測層對路由沒有可測量的影響（v5：四個獨立佐證）

| # | 檢驗 | 結果 |
|---|---|---|
| (a) | 覆蓋率 15.2% → 24.6%（1.6 倍），10 seeds | **零變化**：①②③④ 的 worst-ρ 連標準差都相同（3.3302±0.2703） |
| (b) | 用 `t0` 與用 `tpred` 走出的路徑 | **86% 完全相同** |
| (c) | **換成 gated fusion**（實質不同的預測，15 分鐘 corr 0.985、中位差 3.04%，對照 hybrid↔stgat 的 0.998／0.97%） | ②③④ **仍疊在一起**；④ 在 S2 快 0.71%、S3 慢 1.44%，方向相反 = 雜訊 |
| (d) | **以 fusion 觀測重訓 agent** | 六個 Δ（S2/S3 × 三指標）**全部落在 1σ 內** |

**(c)(d) 是 v5 新增，也是最強的一對**——前兩個只換了覆蓋率或來源，這一次連 agent 都重訓了。

⚠️ **(d) 的限制**：兩個單次訓練的 agent 相比，無法分離「fusion 沒有影響」與
「PPO 的訓練變異蓋過了影響」。要分開得對每個來源各訓數個 seed。
**下方的結構論證是讓前者遠比後者可能的原因，報告應兩者並陳。**

**根因：Dijkstra 比的是時間 = 長度 / 速度，「按長度」的覆蓋率才是有效的那個。** 再簡化只改變邊的計數方式、不移除任何道路，所以**按長度的覆蓋率恆為 14.1%**。把路徑成本拆開（令 $m_e = t^{pred}_e / t^0_e$）：

$$\text{cost}_{pred} = \underbrace{2.035 \times \text{cost}_{t_0}}_{\text{均勻縮放}} + \underbrace{\sum_{e \in \text{有預測}} t^0_e (m_e - 2.035)}_{\text{唯一能改變排序的擾動}}$$

- 無預測的邊全部乘同一常數 2.035（`network_mean` fallback），**均勻縮放不改變路徑排序**。
- 有預測的邊 $m_e = 2.368 \pm 0.789$，但只佔路徑成本的 **14.1%**；一條 29 跳的路徑約經過 7 條有預測的邊，**擾動僅約路徑成本的 2%**，小於競爭路線之間的典型差距。

實測佐證：**用 `t0` 與用 `tpred` 走出完全相同路徑的比例 = 86%**。

### fallback 的兩難：消除偏差與保留訊號互相衝突

| 設定 | 有預測 vs 無預測 | 結果 |
|---|---|---|
| `free_flow` | 24.8 vs 50.5 km/h（差 2 倍） | 路由**系統性避開所有有資料的主幹道**，②③④ 比 ① 慢 18% |
| **`network_mean`（現行）** | 24.8 vs 24.9 km/h（對齊） | 偏差消除，**但訊號一起消失**，②③④ ≡ ① |

**兩種設定都無法讓預測正向影響決策**——這不是設定選錯，而是覆蓋率的本質限制：85% 的路網沒有觀測，任何 fallback 都只能是一個假設，而假設無論偏高、偏低或對齊，都不會產生資訊。

**「縮小場域拉高覆蓋率」這條路已實測失敗**：節點縮到 58%、按邊數的覆蓋率翻倍，②③④ 依然等於 ①。

> TDX 的 202 個路段對應約 600 條邊，而路網要**強連通且提供替代道路**至少需要 1,690 條邊。
> **預測永遠只能覆蓋路徑長度的一小部分。這是結構問題，不是程度問題。**

### 判讀：這反而強化了本專題的核心主張

在**預測貢獻可證明為零**的條件下，⑤⑥⑦ 仍達成大幅改善：

```
                     S2 尖峰漏斗                      S3 主幹道封閉
⑤ 只有協調      ATT −42.9%  ρ −36.2%  Gini −3.3%   ATT −60.4%  ρ −35.7%  Gini −7.7%
⑥ 協調 + eq.4   ATT −44.0%  ρ −41.9%  Gini −18.1%  ATT −61.2%  ρ −43.3%  Gini −17.4%
⑦ DRL（本系統）  ATT −36.0%  ρ −24.6%  Gini −10.1%  ATT −55.8%  ρ −27.5%  Gini −12.9%
```

計畫書的核心論點是「**問題出在決策機制，不在預測準確度**」。
**台中的證據比 METR-LA 更強——預測的邊際貢獻可量化為零，改善只能來自決策層。**

⚠️ **代價**：計畫書「預測 → 決策」的耦合在台中**沒有正向實證**。
**報告寫法**：主結果用 `network_mean`，附 `free_flow` 敏感度分析，明確揭露 14.1%（按長度）覆蓋率的限制，並以 METR-LA（100% 感測器覆蓋，ATT 0.1118 → 0.0349）佐證「覆蓋率足夠時預測確實會改變路由」。

---

## 6. 與計畫書的對應

| 計畫書項目 | 本設計對應 | 狀態 |
|---|---|---|
| §4.2 資料來源與前處理 | §2（含 Gaussian kernel 鄰接） | ✅ |
| §4.3 STGCN + STGAT dual-path | §3.7 | ✅ |
| **§4.3 Gated Fusion（eq.3）** | **§3.7** | ✅ **完成**（聯合訓練，一個模型輸出 15/30/60） |
| §4.3 氣候特徵注入 STGAT | — | ❌ **量化後決定不整合**（效應 0.66 km/h < MAE 3.47 km/h 且非單調），須揭露 |
| §4.4 POMDP + PPO + 全域懲罰 | §4.5 策略 ⑦ | ✅ |
| §4.6 評估指標 | §3.3（預測）、§4.6（決策） | ✅ |
| §4.6 四個基準比較 | §4.5 策略 ①②③⑦ | ✅（擴充為七策略 + 兩種解碼） |
| **§4.6 HA 基準** | **§3.6** | ✅ **完成** |
| §4.5 SUMO | §4.4（BPR 為代理，介面已預留） | ⚠️ 組員 B |
| §5 可移轉性驗證 | §3.1 實驗 A3（改為 fine-tune，取代 zero-shot） | 🔴 **未做——唯一完全未動的要求** |
| §5 三個時程輸出（15/30/60） | §3.7、§4.5 | ✅ 預測層一次 forward 給三個時程；⑦a/⑦b 消融待排期 |
| §5 ATT ↓20–30% | §4.7 | ✅ **S2 −36.0%、S3 −55.8%** |
| §5 worst-ρ ↓20%+ | §4.7 | ✅ **S2 −24.6%、S3 −27.5%** |
| §5 Gini ↓30%+ | §4.7 | ❌ **S2 −10.1%、S3 −12.9%**（**oracle 亦僅 −18.1 / −17.4%**） |
| §5 推論延遲 < 50 ms | — | ⚠️ 未量測 |

---

## 7. 執行順序（v5：主線全部完成）

| 階段 | 工作 | 負責 | 狀態 |
|---|---|---|---|
| **P0–P3b** | 資料鏈：TDX 抓取、路網簡化、幾何／時序分離、mask、格式轉換 | 本人 | ✅ 完成 |
| **P4** | 軌道 A：A2 訓練與 **masked 評估** | 本人 | ✅ 完成（§3.5） |
| **P4b** | 集成權重搜尋（val 挑選 → **test 確認**） | 本人 | ✅ 完成 |
| **P5** | **Gated Fusion** | 本人 | ✅ **完成**（§3.7） |
| **P5b** | **HA 基準** | 本人 | ✅ **完成**（§3.6） |
| **P6–P8** | 實驗場域、容量校準、預測→決策耦合 | 本人 | ✅ 完成 |
| **P9** | 軌道 B：DRL 訓練 + 七策略 × 10 seeds | 本人 | ✅ 完成 |
| **P9b** | **`--togo-refresh 25`** | 本人 | ✅ **完成**（§4.8 ⑤，worst-ρ 因此跨過門檻） |
| **P9c** | **beam search 解碼 + 寬度 1 等價測試** | 本人 | ✅ **完成**（§4.5、§4.8 ⑦） |
| **P10** | **S3 主幹道封閉情境** | 本人 | ✅ **完成**（§4.3、§4.7） |
| **P10b** | **fusion 接進決策層並重訓** | 本人 | ✅ **完成**（§5 佐證 (d)） |
| **P11** | **A3 遷移實驗** | 本人 | 🔴 **待做** |
| **P12** | ⑦a 單時程 vs ⑦b 三時程消融 | 本人 | ⚠️ 待排期（fusion 完成後已可行） |
| **P12b** | fusion 消融：不帶 `--stgcn-tod` 重跑 | 本人 | ⚠️ 待做（§3.7 ④ 的歸因目前是推論） |
| **P13** | SUMO / TraCI 取代 BPR | 組員 B | ⚠️ 未開始 |

**里程碑對應**：P0–P5b → M2（預測模組）；P6–P10b → M3/M4（決策 + 基準比較）；P13 → M3（SUMO）。

---

## 8. 需要的程式改動

### 8.1 已完成

| 改動 | 檔案 | 對應 |
|---|---|---|
| 資料鏈重構（幾何／時序分離） | `build_network.py`、`build_speed.py` | §2.5 |
| 路網簡化（含旅行時間不變性證明） | `build_simplified_network.py` | §2.5 |
| 鄰接距離門檻 κ（對齊 METR-LA 密度 37.56%） | `build_network.py` | §2.4 |
| 一對多 mapping（715 / 601 邊） | `section_to_edges.csv` | §5 |
| 缺值 mask 輸出 | `build_speed.py` | §2.3 |
| **mask 評估**（含 `--n-pred` 檔名守門） | `STGCN/evaluate_masked.py`、`STGAT/evaluate_masked_taichung.py` | §3.3 |
| 集成權重搜尋（含誤差相關係數） | `search_ensemble_weight.py` | §3.2 |
| **HA 基準 + 異常度分桶 + persistence 交叉驗證** | **`ha_baseline.py`** | **§3.6** |
| **Gated Fusion：別名載入、統一 dataloader、閘控、訓練、評估** | **`fusion/`（1,241 行）** | **§3.7** |
| **管線等價驗收** | **`fusion/verify.py`** | §3.7 ③ |
| 實驗場域 + 取子圖後再簡化 | `TDX_Data/build_arena.py` | §4.1 |
| 推論 → DRL 輸入的時間對齊（硬守門） | `run_infer_taichung.py --target-row`、`make_drl_input.py` | §5 |
| **fusion → 決策層**（`--source fusion` + CSV sidecar） | `make_drl_input.py` | §5 (d) |
| `served%` + `NOT COMPARABLE` 標記 | `run_compare.py` | §4.6 |
| 字典序 checkpoint 選取 | `train_drl.py` | §4.6 |
| `.meta.json` sidecar（checkpoint 與預測 CSV） | `train_drl.py` / `make_drl_input.py` | §4.6 |
| agent 歸因診斷（繞路／壅塞／折損，**多 agent 同一次執行**） | `diagnose_agent.py` | §4.8 |
| `--togo-refresh` | `policies.py`、`train_drl.py` | §4.8 ⑤ |
| **S3 事件注入**（`--close-road/-edge/-busiest/-at/-demand/-list` + 兩道守門） | **`closure.py`** | **§4.3** |
| **beam search 解碼** | `policies.py`、`run_compare.py --beam` | §4.5 |
| **寬度 1 等價測試** | **`test_beam.py`** | §4.5 |

### 8.2 待做

| 改動 | 檔案 | 對應 | 優先度 |
|---|---|---|---|
| **部分權重載入邏輯（A3 fine-tune）** | STGAT／STGCN 訓練腳本 | §3.1 | 🔴 計畫書明文要求，唯一完全未動的 |
| fusion 消融：`--stgcn-tod` 開關對照 | `fusion/train.py`（旗標已有） | §3.7 ④ | 高——目前的歸因是推論 |
| 推論延遲量測（< 50 ms） | 新增 | §6 | 中——計畫書 §5 有此指標 |
| 訓練 log 的 `served` 改一位小數 | `train_drl.py` | §4.6 | 低 |
| checkpoint 定期存檔（非只留最佳） | `train_drl.py` | §4.7 | 低 |
| **效能**：PPO update 批次化 | `policies.py` | §9 | 低（瓶頸是 kernel launch，非算力） |

### 8.3 🔴 已知但刻意不改的

| 項目 | 說明 |
|---|---|
| **選取規則偏向門檻邊緣** | score 的三項都只在已送達的車上計算，而 served 懲罰**只在低於門檻時**生效，所以放棄 5% 最難的行程會同時美化三項而無補償。實測：served 95% 的 checkpoint 平均 +0.258、99% 的僅 +0.230（單調遞減），**字典序必然挑中卡在 95% 的那個**。beam-8 之後兩者皆 99.9%，報告數字不受影響，故記錄而不修（修法：合格者中分數差在 1σ 內就取 served 最高者，約 5 行） |
| **失敗行程的負載不計入指標、卻計入決策** | `step()` 逐跳記負載，故失敗的行程仍佔用道路並影響後續車輛；但 `metrics.edge_loads` 從回傳的 paths 算，`None` 不計。beam search **照抄**此行為以維持可比 |
| `node_dyn[:,2]`（平均出向飽和度）不排除封閉邊 | S3 為零樣本評估，觀測分佈越接近訓練時越好 |

---

## 9. 硬體與效能

**結論：現有 RTX 3080（10 GB）足夠，瓶頸不在 GPU。**

profiling（840 節點場域，800 車）：

| 項目 | 成本 | 判讀 |
|---|---|---|
| `_compute_enc_ctx` | 2.27 ms/車 → **1.8 s/episode** | 不是瓶頸 |
| `_observe` | 0.01 ms/步 → 0.4 s | 不是瓶頸 |
| **PPO update** | 800 車 × 48 步 × 4 epochs = **153,600 次獨立 GPU forward** | 🔴 **瓶頸** |
| VRAM | 4.2 / 10 GB，GPU 未飽和 | 加大 batch 無效 |

單次 forward 的運算是微秒級，但 **kernel launch 開銷約 50–100 µs**——時間幾乎全花在發射 15 萬次小工作上。**加大 batch size 不會減少發射次數**；正確做法是 **padding + masking 批次化**，或每 K 台車才重編碼一次。

> **場域再簡化（§4.1 步驟 ④）本身就是最有效的優化**：平均路徑 46.8 → 28.7 跳，
> transition 數直接少 39%，同時把「沒得選的決策點」從 54.5% 降下來——
> **既省時間又改善梯度品質。**

---

## 10. 待確認事項與風險

| # | 項目 | 狀態 |
|---|---|---|
| 1–11 | 取樣間隔／覆蓋率分母／節點集／缺值填補／鄰接稠密／速限／多 seed 統計／異常值／分離基準 ②③／`n_vertex`／mask 接線 | ✅ 全部已解決（處置見 §2–§4） |
| 12 | 26 個路段真實觀測率 < 60% | ⚠️ 保留於資料集；MAE 異常時優先查此處 |
| 13 | 時間戳為 UTC（台灣 +8） | ⚠️ 尖峰／星期分析前必須轉換（HA 與 fusion 皆已處理） |
| 14 | 容量縮放為經驗值（0.0429 對應 800 車） | ⚠️ 車輛數改變需重跑 `calibrate_taichung.py` |
| 15 | 集成權重 | ✅ **已在測試集確認**（0.20 vs 0.15，差 0.07%，§3.2） |
| 16 | 軌道 B 的需求為合成 | ✅ **設計上的必然**（§1.1），須在報告主動說明 |
| 17 | SUMO 尚無產出 | ⚠️ BPR 為代理；介面已預留（組員 B） |
| 18 | **Gated Fusion** | ✅ **完成**（§3.7）。⚠️ 效益僅 60 分鐘的 −1.9% 站得住；15 分鐘打平 |
| 19 | **HA 基準** | ✅ **完成**（§3.6） |
| 20 | 🔴 **A3 遷移實驗未做** | **計畫書 §5，唯一完全未動的要求** |
| 21 | 氣候特徵（CWA 降雨）**評估後決定不整合** | ✅ 已量化：0–5 mm/h 有 −2.4~−2.9% 效應但**非單調**，效應量（0.66 km/h）遠小於模型 MAE（3.47 km/h）。**報告須揭露此決策與依據，不可寫成「無影響」** |
| 22 | 🔴 **預測層對路由無可測量影響** | **四個獨立佐證**（§5）。關鍵是**按長度**的覆蓋率恆為 14.1%。**資料可得性的限制，非參數問題** |
| 22b | **`tpred` fallback 的兩難** | `free_flow` 有偏差、`network_mean` 無訊號。報告應兩者並列作敏感度分析 |
| 22c | 🔴 **Gini 未達計畫書門檻** | ⑦ S2 −10.1%、S3 −12.9%，目標 −30%。**oracle 亦僅 −18.1 / −17.4%，故非學習不足**。結構性原因：起點已較均勻（0.71 vs METR-LA 0.87）＋ hotspot 灌向 4 個 hub 的不可避瓶頸。**S3 已實測無法改善**（oracle 的 Gini 反而略差）。剩餘方向：加大 `N_HOTSPOTS` |
| 22d | **worst-ρ** | ✅ **已達標**：S2 −24.6±2.0%、S3 −27.5±2.9%（v4 曾為 −19.8%，恰在門檻上） |
| 22e | **ATT 的 Δ 曾被 attrition 灌水** | ✅ **beam-8 解決**：S2 4.6% 放棄 → 灌水 2.4pp；S3 17.2% → **7.2pp，足以「打贏」oracle**（§4.6）。**報告一律引用 beam-8 列** |
| 23 | **agent 落後 oracle 的原因** | ✅ 已歸因並修正一半：`--togo-refresh` 讓**繞路統計與 oracle 逐位相同**，但 ATT 仍差 11.5%（§4.8 ⑥）。**剩餘差距是「繞去哪」不是「繞多少」**，且 beam search 證實**不是搜尋誤差** |
| 24 | 簡化捨棄 60 組平行鏈（取短者） | ⚠️ 佔 27,022 條的 0.2%，但本專題主張正是「有無替代道路可分流」，須知悉 |
| 25 | 部分節點不在最大 SCC | ✅ 正確套用單行道後的真實結果，非錯誤 |
| 26 | 舊 TDX 金鑰仍在 git 歷史 | ⚠️ 金鑰已改讀 `.env`，但**建議至 TDX 後台 rotate** |
| 27 | **E-GAT 的長路徑 credit assignment** | ⚠️ 平均 28.7 跳（METR-LA 僅 1.5 跳）。已由 per-vehicle GAE + trip-scaled 獎勵 + beam 解碼處理至 served 99.9%，但仍是最大的未知數 |
| **28** | ⚠️ **fusion 的歸因是推論，不是實測** | §3.7 ④ 判定「改善主因是 time-of-day 而非閘控」，依據是分桶表的方向。**直接量測需要 `--stgcn-tod` 開關的對照組**（P12b） |
| **29** | ⚠️ **「fusion 對決策層無影響」建立在兩個單次訓練的 agent 上** | 無法分離「無影響」與「PPO 訓練變異」。要分開得對每個來源各訓數個 seed（§5） |
| **30** | 🔴 **checkpoint 選取偏向門檻邊緣** | 已知並記錄，**刻意不修**（§8.3） |

### 10.1 目前的阻擋路徑

```
🔴 P11   A3 遷移實驗        → 計畫書 §5，唯一完全未動的要求
⚠️ P12b  fusion 消融        → §3.7 ④ 的歸因目前是推論
⚠️ P12   ⑦a/⑦b 三時程消融   → fusion 完成後已可行，需一次完整訓練
⚠️ P13   SUMO               → 組員 B
```

> **不再阻擋的**（v4 時的三個）：`--togo-refresh` 重訓 ✅、S3 ✅、HA ✅。

---

## 11. 一頁摘要（可直接放入報告）

```
                    ┌──────────────────────────────────────────────┐
                    │  軌道 A：預測（202 TDX sections, 176 天）      │
                    │  A1 METR-LA        → 3.18 MAE（文獻可比）✅   │
                    │  A2 TDX scratch    → STGAT 3.38/3.51/3.63 ✅  │
                    │  A3 fine-tune      → 可移轉性驗證 🔴 未做      │
                    │  ★ Gated Fusion（計畫書 §4.3）✅              │
                    │      一個模型 → 3.38 / 3.48 / 3.56            │
                    │  地板：HA 4.05 · persistence 4.29–5.13        │
                    │  ★ 一律以 mask.npy 只在真實觀測(73.8%)計分     │
                    └────────────────┬─────────────────────────────┘
                                     │ 一對多 map-matching
                                     │ 601 條種子邊 → 場域內 416 條
                                     ▼
                    ┌──────────────────────────────────────────────┐
                    │  軌道 B：決策（台中場域 840 節點 / 1,690 邊）  │
                    │  情境：S1 正常 / S2 尖峰漏斗 ✅ / S3 封閉 ✅   │
                    │  壅塞：BPR（capacity_scale 0.0429）→ SUMO     │
                    │  七策略消融 ×  兩種解碼（貪婪 / beam-8）：     │
                    │   ① static ② STGCN ③ STGAT ④ Hybrid          │
                    │   ⑤ load-aware ⑥ oracle ⑦ DRL(本系統)        │
                    │  指標：served·ATT·Gini·worst-ρ·throughput     │
                    │  協定：10 seeds 配對 Δ → mean ± std ✅        │
                    │                                              │
                    │           S2 尖峰漏斗    S3 主幹道封閉         │
                    │  ATT      −36.0% ✅      −55.8% ✅            │
                    │  worst-ρ  −24.6% ✅      −27.5% ✅            │
                    │  Gini     −10.1% ❌      −12.9% ❌            │
                    │  served    99.9%          96.0%              │
                    └──────────────────────────────────────────────┘
```

**核心論述（四句）**：

1. **環境真實、需求合成**——OSM 拓樸 + TDX 車速 + 車道容量皆為實測；需求因無真實 OD
   且需反事實比較而合成。所有策略在**完全相同的環境與需求**下對照，這是交通模擬研究的
   標準協定。

2. **問題出在決策機制，不在預測準確度**——在台中，預測對路由的邊際貢獻**可量化為零**
   （四個獨立佐證，§5），因為按長度的覆蓋率只有 14.1%。而協調與全域懲罰仍帶來
   **ATT −36%、worst-ρ −25%、Gini −10%**（事故情境下 ATT 更達 −56%）。
   **改善只能來自決策層。**

3. **羊群效應不只讓通行時間變差，還讓它變得不可預測**——未協調策略的 ATT 標準差為
   ±161.6，協調後降至 ±9.8（**16.4 倍**）。對導航服務而言，「今天多久到」無法估計
   本身就是嚴重的服務品質問題。

4. **未達標的那一項，上界自己也達不到**——Gini 目標 −30%，本系統 −10~−13%，
   **但解析式 oracle 也只有 −17~−18%**。這把差距定位在**情境結構**（起點分佈已較均勻、
   hotspot 灌向 4 個 hub 的不可避瓶頸），而非模型的學習能力。

### 三個必須寫進報告的方法論註記

| | 註記 |
|---|---|
| **A** | **⑦ 一律引用 beam-8 那一列。** 貪婪解碼在 S3 的 ATT Δ 是 −63.0%，**看似打贏 oracle 的 −61.2%**——那是 17.2% 放棄率造成的 7.2pp 灌水（§4.6） |
| **B** | **計畫書 §4.3 的雙路前提，有一半原本不成立。** STGCN 缺 time-of-day，在最 routine 的分桶與平日尖峰**輸給 HA**；補上它自己就要求的輸入後才成立（§3.6 ③、§3.7 ④） |
| **C** | **預測層無貢獻是資料可得性的限制，不是方法失敗。** 須揭露 14.1% 的按長度覆蓋率，並以 METR-LA（100% 感測器覆蓋，ATT 0.1118 → 0.0349）佐證覆蓋率足夠時預測確實改變路由 |
